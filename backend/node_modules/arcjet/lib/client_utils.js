"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.assert = (assertion, message) => {
    if (assertion === false)
        throw new Error(`Assertion Error: ${message}`);
};
exports.hexToBytes = (hex) => {
    const byteArray = new Uint8Array(hex.length / 2);
    for (let c = 0, h = 0; c < hex.length; c += 2, h++) {
        byteArray[h] = parseInt(hex.substr(c, 2), 16);
    }
    return byteArray;
};
exports.bytesToHex = (arr) => {
    const hex = [];
    for (let c = 0; c < arr.length; c++) {
        hex.push(arr[c].toString(16).padStart(2, '0'));
    }
    return hex.join('');
};
//# sourceMappingURL=client_utils.js.map