"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const express = require("express");
exports.server = (store, port) => new Promise((resolve, reject) => {
    const app = express();
    app.get('/:hash', (req, res) => __awaiter(this, void 0, void 0, function* () {
        try {
            const { data, error } = yield store.get(req.params.hash);
            if (error && error === 'NOTFOUND') {
                res.status(404).send('Not Found');
            }
            else {
                res.status(200).send(data);
            }
        }
        catch (err) {
            console.error(err);
            res.status(500).send(err);
        }
    }));
    app.post('/', (req, res) => __awaiter(this, void 0, void 0, function* () {
        try {
            const hash = yield store.set(req.body);
            res.status(200).send(hash);
        }
        catch (err) {
            console.error(err);
            res.status(500).send(err);
        }
    }));
    app.listen(port, () => {
        resolve();
    });
});
//# sourceMappingURL=server.js.map