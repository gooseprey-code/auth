"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = require("assert");
const program = require("commander");
const store_1 = require("./store");
const server_1 = require("./server");
const pkg = require("../package.json");
program.version(pkg.version, '-v, --version');
program
    .command('start <file>')
    .description('Start server with a database file')
    .option('-p', '--port <port>', 'Port number to listen on')
    .action((file, options) => __awaiter(this, void 0, void 0, function* () {
    try {
        assert_1.strict.ok(options.port);
        const store = new store_1.default();
        yield store.init(file);
        yield server_1.server(store, options.port);
    }
    catch (err) {
        console.error(err);
    }
}));
//# sourceMappingURL=arcjet.js.map