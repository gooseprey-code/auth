#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const program = require("caporal");
const fs = require("fs");
const path = require("path");
const got = require("got");
const store_1 = require("./store");
const server_1 = require("./server");
const pkg = require('../package.json');
const DEFAULT_PORT = 3000;
program.version(pkg.version);
program
    .command('start', 'Start server with a database file')
    .argument('<file>', 'File to start the database with')
    .option('--port <port>', 'Port number to listen on', program.INT, 3000, true)
    .action((args, options, logger) => __awaiter(this, void 0, void 0, function* () {
    try {
        const store = new store_1.default();
        yield store.init(args.file);
        yield server_1.server(store, parseInt(options.port, 10) || DEFAULT_PORT);
        logger.info('Server started');
    }
    catch (err) {
        logger.error(err);
    }
}));
program
    .command('set', 'Add a file to the Arcjet network')
    .argument('<file>', 'File to add to the network')
    .option('--port <port>', 'Port number to connect to', program.INT, 3000, true)
    .action((args, options, logger) => __awaiter(this, void 0, void 0, function* () {
    try {
        const request = got.stream(`http://127.0.0.1:${options.port || DEFAULT_PORT}/`, {
            method: 'POST',
            encoding: 'utf8',
        });
        const stream = fs.createReadStream(path.resolve(process.cwd(), args.file), 'utf8');
        stream.pipe(request);
        request.on('response', response => {
            response.on('data', data => {
                logger.info(data.toString());
            });
        });
    }
    catch (err) {
        logger.error(err);
    }
}));
program.parse(process.argv);
//# sourceMappingURL=arcjet.js.map